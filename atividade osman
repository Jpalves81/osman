#include <stdio.h>

int main ()
{
int count;

       for (count=5; count >= 1; count--)
       {
          printf("%d\n", count);
       }
       for (count=1; count <=5; count++)
       {
           printf("%d\n", count);
       }
    return 0;
}
